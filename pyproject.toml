[project]
name = "mcp-server2"
version = "0.1.0"
description = "Local File MCP Server for ChatGPT"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0"
]

[project.scripts]
mcp-local-files = "mcp_server.main:main"
